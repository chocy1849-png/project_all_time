namespace ProjectAllTime.VN.Dialogue
{
    /// <summary>Origin of a request through the project's single line advance path.</summary>
    public enum VNAdvanceSource
    {
        Manual,
        Auto,
        Skip,
    }
}
